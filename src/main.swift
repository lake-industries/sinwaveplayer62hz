import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello, iOS App Publisher!")
                .font(.title)
            Spacer()
        }
        .padding()
    }
}

#Preview {
    ContentView()
}